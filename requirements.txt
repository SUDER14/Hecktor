# Stack per CLAUDE.md. Training runs on Google Colab Pro; this file is what
# notebooks/colab_train_baseline.ipynb installs there. Local dev should use
# the same versions so a script that runs locally behaves the same on Colab.
torch>=2.1
monai>=1.3
nibabel
SimpleITK
numpy
scipy
pandas
scikit-image
matplotlib
wandb
pyyaml
pytest
